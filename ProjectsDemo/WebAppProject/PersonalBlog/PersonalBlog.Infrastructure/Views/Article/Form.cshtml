@model PBlog.DomainEntities.Article

<div class="container">
    <h1>@(Model.Id == Guid.Empty ? "Create New Article" : "Edit Article")</h1>

    <form method="post" action="@(Model.Id == Guid.Empty ? Url.Action("Add") : Url.Action("Edit"))">
        @if (Model.Id != Guid.Empty)
        {
            <input type="hidden" asp-for="Id" />
        }

        <div class="mb-3">
            <label asp-for="Title" class="form-label">Title</label>
            <input asp-for="Title" class="form-control" required />
            <span asp-validation-for="Title" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <label asp-for="Content" class="form-label">Content</label>
            <textarea asp-for="Content" class="form-control" rows="10" required></textarea>
            <span asp-validation-for="Content" class="text-danger"></span>
        </div>

        <div class="mb-3">
            <button type="submit" class="btn btn-primary">
                @(Model.Id == Guid.Empty ? "Create" : "Update")
            </button>
            <a href="@Url.Action("Index")" class="btn btn-secondary">Cancel</a>
        </div>
    </form>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
    <!-- Thêm rich text editor nếu cần -->
}